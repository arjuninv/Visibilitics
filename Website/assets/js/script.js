document.addEventListener("DOMContentLoaded", function() {
  load();
var input = document.getElementById("inlineFormInputGroup");

/*
var u1 = document.getElementById("user1");
var u2 = document.getElementById("user2");


if(urlParams.has('u')){
  if(urlParams.get('u') == "2") {
    u1.style.textDecoration = "none";
    u2.style.textDecoration = "underline overline";;
  }
} else {
  u1.style.textDecoration = "underline overline";;
  u2.style.textDecoration = "none";;
}
*/
});

function load() {
  /*
	var theDiv = document.getElementById("searchresults");
  theDiv.innerHTML = "";
	var search = document.getElementById("inlineFormInputGroup").value;

var xmlhttp = new XMLHttpRequest();
		xmlhttp.onreadystatechange = function() {
		if (this.readyState == 4 && this.status == 200) {
		    var myArr = JSON.parse(this.responseText);
		   	console.log("l = " + myArr.rows.length);
		   	 	for(var i=0; i< myArr.rows.length; i++) {
            console.log(myArr.rows[i][1]);
            theDiv.innerHTML+=  '<div class="card"> <div class="card-header mb-5"> <h3 class="card-title">' + myArr.rows[i][1] + '</h3> <h5 class="card-category">' + myArr.rows[i][4] + ' bids | Base: ' + myArr.rows[i][5] + ' | Highest: ' + myArr.rows[i][3] + '</h5> </div> <div class="card-body"> <p>' + myArr.rows[i][2]+ '</p><br> <button class="btn btn-primary btn-simple">Bid</button></div> </div>';
			   	}
		    }

		};

		xmlhttp.open("GET", url, true);
		xmlhttp.send();

*/
}
